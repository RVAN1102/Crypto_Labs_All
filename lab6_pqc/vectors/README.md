# Test vectors

Official NIST or ACVP ML-DSA/ML-KEM vectors are not embedded in this
repository. Current tests are OpenSSL-generated roundtrips and negative
tests; they must not be described as official KAT coverage.

A future vector runner must document its schema and provenance and must fail
closed on malformed input.

