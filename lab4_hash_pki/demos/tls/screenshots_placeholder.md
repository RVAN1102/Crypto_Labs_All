# TLS Screenshots Placeholder

No screenshots are included yet because no trusted-root owned-domain deployment is claimed.

When a real deployment is available, add screenshots or exported evidence showing:

- Apache or Nginx serving the owned domain.
- TLS 1.2 or TLS 1.3 negotiated.
- Browser or OS trust chain validation to a trusted root.
- Certificate SAN matching the owned hostname.
- Certificate validity period.
- Certificate chain details, including leaf and intermediate certificates.

Do not include screenshots from third-party targets.

